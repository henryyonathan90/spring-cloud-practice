spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        prefix: config
        defaultContext: apps
        profile-separator: '::'
        format: files
      discovery:
        service-name: practice

  application:
    name: spring-cloud-practice
